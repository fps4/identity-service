{
  "name": "@fps4/identity-service-react",
  "version": "0.2.0",
  "description": "Optional React UI for identity-service — drop-in <Login/> and <Register/> for consumer apps (RQ-0003 / RQ-0015). Separate from the headless @fps4/identity-service-sdk SDK so server-side consumers never pull in React.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "react": "^19.0.0",
    "typescript": "^5.4.5",
    "vitest": "^3.2.4"
  }
}
